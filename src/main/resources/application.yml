spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/cc
    username: root
    password: hunil3751

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: ture

aws:
  access-key:
  secret-key:
  s3:
    bucket: test
    base-url: test

logging.level:
  org.hibernate.SQL: debug